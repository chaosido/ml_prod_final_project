apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://asr-prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "30s"
      httpMethod: POST

